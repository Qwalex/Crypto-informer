<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Настройки Криптовалютного Бота</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #2c3e50, #34495e);
            color: white;
            padding: 30px;
            text-align: center;
        }

        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 1.1rem;
            opacity: 0.9;
        }

        .nav-tabs {
            display: flex;
            background: #f8f9fa;
            border-bottom: 1px solid #ddd;
        }

        .nav-tab {
            flex: 1;
            padding: 20px;
            text-align: center;
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            color: #666;
            transition: all 0.3s ease;
        }

        .nav-tab.active {
            background: white;
            color: #2c3e50;
            border-bottom: 3px solid #3498db;
        }

        .nav-tab:hover {
            background: #e9ecef;
        }

        .tab-content {
            display: none;
            padding: 40px;
        }

        .tab-content.active {
            display: block;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #2c3e50;
            font-size: 1rem;
        }

        .form-group input, .form-group select, .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }

        .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
        }

        .form-group small {
            display: block;
            margin-top: 5px;
            color: #666;
            font-size: 0.9rem;
        }

        .button {
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            transition: all 0.3s ease;
            margin-right: 10px;
        }

        .button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(52, 152, 219, 0.3);
        }

        .button.secondary {
            background: linear-gradient(135deg, #95a5a6, #7f8c8d);
        }

        .button.success {
            background: linear-gradient(135deg, #27ae60, #229954);
        }

        .button.danger {
            background: linear-gradient(135deg, #e74c3c, #c0392b);
        }

        .exchange-checkbox {
            margin-bottom: 15px;
            padding: 15px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            background: #f8f9fa;
            transition: all 0.3s ease;
        }

        .exchange-checkbox:hover {
            border-color: #3498db;
            background: #f0f8ff;
        }

        .exchange-checkbox.selected {
            border-color: #27ae60;
            background: #e8f5e8;
        }

        .exchange-checkbox input[type="checkbox"] {
            margin-right: 10px;
            transform: scale(1.2);
        }

        .exchange-checkbox label {
            cursor: pointer;
            margin: 0;
            font-weight: normal;
        }

        .pairs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 10px;
            margin-top: 15px;
        }

        .pair-checkbox {
            display: flex;
            align-items: center;
            padding: 10px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .pair-checkbox:hover {
            border-color: #3498db;
            background: #f8f9fa;
        }

        .pair-checkbox.selected {
            border-color: #27ae60;
            background: #d5f4e6;
        }

        .pair-checkbox input {
            margin-right: 8px;
            width: auto;
        }

        .pair-dropdown-item {
            padding: 10px 15px;
            cursor: pointer;
            border-bottom: 1px solid #f0f0f0;
            transition: background 0.2s ease;
        }

        .pair-dropdown-item:hover {
            background: #f8f9fa;
        }

        .pair-dropdown-item:last-child {
            border-bottom: none;
        }

        .selected-pair-tag {
            display: inline-flex;
            align-items: center;
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            padding: 8px 12px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .selected-pair-tag .remove-pair {
            margin-left: 8px;
            cursor: pointer;
            font-weight: bold;
            opacity: 0.8;
            transition: opacity 0.2s ease;
        }

        .selected-pair-tag .remove-pair:hover {
            opacity: 1;
        }

        .alert {
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
            font-weight: 500;
        }

        .alert.success {
            background: #d5f4e6;
            color: #27ae60;
            border: 1px solid #27ae60;
        }

        .alert.error {
            background: #fdf2f2;
            color: #e74c3c;
            border: 1px solid #e74c3c;
        }

        .alert.warning {
            background: #fff5d7;
            color: #f39c12;
            border: 1px solid #f39c12;
        }

        .alert.info {
            background: #e8f4f8;
            color: #3498db;
            border: 1px solid #3498db;
        }

        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid #f3f3f3;
            border-top: 3px solid #3498db;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-right: 10px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .instructions {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
        }

        .instructions h3 {
            color: #2c3e50;
            margin-bottom: 15px;
        }

        .instructions ol {
            padding-left: 20px;
        }

        .instructions li {
            margin-bottom: 10px;
            line-height: 1.6;
        }

        .instructions a {
            color: #3498db;
            text-decoration: none;
        }

        .instructions a:hover {
            text-decoration: underline;
        }

        .chat-id-finder {
            background: #e8f4f8;
            padding: 20px;
            border-radius: 10px;
            border: 1px solid #3498db;
        }

        .status-indicator {
            display: inline-block;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            margin-right: 8px;
        }

        .status-indicator.green {
            background: #27ae60;
        }

        .status-indicator.red {
            background: #e74c3c;
        }

        .status-indicator.yellow {
            background: #f39c12;
        }

        @media (max-width: 768px) {
            .container {
                margin: 10px;
                border-radius: 15px;
            }

            .header {
                padding: 20px;
            }

            .header h1 {
                font-size: 2rem;
            }

            .tab-content {
                padding: 20px;
            }

            .nav-tabs {
                flex-direction: column;
            }

            .pairs-grid {
                grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🤖 Криптовалютный Бот</h1>
            <p>Панель управления настройками</p>
        </div>

        <div class="nav-tabs">
            <button class="nav-tab active" onclick="showTab('telegram')">📱 Telegram</button>
            <button class="nav-tab" onclick="showTab('exchange')">🏦 Биржа</button>
            <button class="nav-tab" onclick="showTab('analysis')">📊 Анализ</button>
            <button class="nav-tab" onclick="showTab('status')">⚙️ Статус</button>
        </div>

        <form id="configForm">
            <!-- Telegram Tab -->
            <div id="telegram" class="tab-content active">
                <div class="instructions">
                    <h3>📱 Настройка Telegram бота</h3>
                    <ol>
                        <li>Перейдите в Telegram к <a href="https://t.me/BotFather" target="_blank">@BotFather</a></li>
                        <li>Отправьте команду <code>/newbot</code></li>
                        <li>Следуйте инструкциям и получите Bot Token</li>
                        <li>Вставьте токен в поле ниже и нажмите "Проверить токен"</li>
                        <li>После проверки токена отправьте любое сообщение вашему боту</li>
                        <li>Нажмите "Найти Chat ID" для автоматического определения</li>
                    </ol>
                </div>

                <div class="form-group">
                    <label for="telegramBotToken">Bot Token *</label>
                    <input type="text" id="telegramBotToken" name="telegramBotToken" 
                           placeholder="123456789:ABCdefGHijklmnopQRSTuvwxyz" required>
                    <small>Токен получен от @BotFather</small>
                </div>

                <div style="margin-bottom: 20px;">
                    <button type="button" class="button" onclick="validateToken()">
                        <span id="tokenValidationLoading" class="loading" style="display: none;"></span>
                        Проверить токен
                    </button>
                    <div id="tokenValidationResult"></div>
                </div>

                <div class="chat-id-finder">
                    <h3>🔍 Поиск Chat ID</h3>
                    <p>Сначала отправьте любое сообщение вашему боту в Telegram, затем нажмите кнопку ниже:</p>
                    <br>
                    <button type="button" class="button success" onclick="findChatId()">
                        <span id="chatIdLoading" class="loading" style="display: none;"></span>
                        Найти Chat ID
                    </button>
                    <div id="chatIdResult" style="margin-top: 15px;"></div>
                </div>

                <div class="form-group">
                    <label for="telegramChatId">Chat ID *</label>
                    <input type="text" id="telegramChatId" name="telegramChatId" 
                           placeholder="123456789" required>
                    <small>ID чата для отправки уведомлений</small>
                </div>

                <div style="margin-top: 20px;">
                    <button type="button" class="button success" onclick="sendTestMessage()">
                        <span id="testMessageLoading" class="loading" style="display: none;"></span>
                        Отправить тестовое сообщение
                    </button>
                    <div id="testMessageResult"></div>
                </div>
            </div>            <!-- Exchange Tab -->
            <div id="exchange" class="tab-content">
                <div class="instructions">
                    <h3>🏦 Настройка бирж</h3>
                    <p>Выберите биржи для мониторинга и получения данных о валютных парах. 
                       API ключи опциональны и нужны только для расширенной информации.</p>
                </div>                <!-- Выбор бирж -->
                <div class="form-group">
                    <label>Выберите биржи для мониторинга * (106 доступных бирж)</label>
                    <small>Выберите одну или несколько бирж для получения данных о валютных парах. Поддерживаются все биржи из библиотеки ccxt.</small>
                    
                    <!-- Фильтры по категориям -->
                    <div style="margin: 15px 0;">
                        <button type="button" class="button secondary" onclick="showExchangeCategory('top')">ТОП-10 бирж</button>
                        <button type="button" class="button secondary" onclick="showExchangeCategory('derivatives')">Деривативы</button>
                        <button type="button" class="button secondary" onclick="showExchangeCategory('regional')">Региональные</button>
                        <button type="button" class="button secondary" onclick="showExchangeCategory('altcoins')">Альткоины</button>
                        <button type="button" class="button secondary" onclick="showExchangeCategory('all')">Все биржи</button>
                    </div>

                    <!-- Поиск по биржам -->
                    <div style="margin: 15px 0;">
                        <input type="text" id="exchangeSearch" placeholder="Поиск по названию биржи..." 
                               style="padding: 8px; border: 1px solid #ddd; border-radius: 5px; width: 300px;"
                               oninput="filterExchanges()">
                    </div>

                    <!-- Контейнер для бирж -->
                    <div id="exchangesContainer" style="margin-top: 15px; display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 8px; max-height: 400px; overflow-y: auto; border: 1px solid #eee; padding: 15px; border-radius: 8px;">
                        <!-- Биржи будут добавлены через JavaScript -->
                    </div>
                    
                    <div style="margin-top: 15px;">
                        <button type="button" class="button secondary" onclick="selectTopExchanges()">Выбрать ТОП-5 бирж</button>
                        <button type="button" class="button secondary" onclick="selectAllVisible()">Выбрать все видимые</button>
                        <button type="button" class="button secondary" onclick="clearAllExchanges()">Очистить выбор</button>
                        <span id="selectedCount" style="margin-left: 15px; font-weight: bold;">Выбрано: 0</span>
                    </div>
                </div>                <!-- Информация о публичных данных -->
                <div class="alert success" style="margin-top: 20px;">
                    <h4>📊 Работа с публичными данными</h4>
                    <p><strong>API ключи НЕ ТРЕБУЮТСЯ!</strong> Система анализирует публичные рыночные данные:</p>
                    <ul>
                        <li>📈 Цены и объемы торгов</li>
                        <li>📊 Технические индикаторы (RSI, MACD, Bollinger Bands)</li>
                        <li>🔄 Исторические данные для анализа</li>
                        <li>🌍 Данные с нескольких бирж одновременно</li>
                    </ul>
                    <p>Просто выберите биржи выше и нажмите "Сохранить настройки"!</p>
                </div>            </div>

            <!-- Analysis Tab -->
            <div id="analysis" class="tab-content">
                <div class="instructions">
                    <h3>📊 Настройка анализа рынка</h3>
                    <p>Выберите интервал анализа и валютные пары для мониторинга. 
                       Рекомендуется использовать интервал 4 часа для swing-торговли.</p>
                </div>

                <div class="form-group">
                    <label for="analysisInterval">Интервал анализа *</label>
                    <select id="analysisInterval" name="analysisInterval" required>
                        <option value="15m">15 минут</option>
                        <option value="30m">30 минут</option>
                        <option value="1h">1 час</option>
                        <option value="2h">2 часа</option>
                        <option value="4h" selected>4 часа (рекомендуется)</option>
                        <option value="6h">6 часов</option>
                        <option value="12h">12 часов</option>
                        <option value="1d">1 день</option>
                    </select>
                    <small>Временной интервал для технического анализа</small>
                </div>                <div class="form-group">
                    <label>Валютные пары для анализа *</label>
                    <small>Выберите валютные пары для мониторинга (рекомендуется 5-15 пар)</small>
                    
                    <!-- Поле поиска и добавления пар -->
                    <div style="margin-top: 10px; margin-bottom: 15px;">
                        <div style="display: flex; gap: 10px; align-items: center;">
                            <input type="text" id="pairSearch" placeholder="Найти пару (например: BTC)" 
                                   style="flex: 1; padding: 10px; border: 2px solid #e0e0e0; border-radius: 8px;"
                                   oninput="filterPairs()" onfocus="showDropdown()" onblur="hideDropdown()">
                            <button type="button" class="button secondary" onclick="showAllPairs()">Показать все пары</button>
                        </div>
                        
                        <!-- Выпадающий список найденных пар -->
                        <div id="pairDropdown" style="display: none; max-height: 200px; overflow-y: auto; border: 2px solid #3498db; border-radius: 8px; background: white; margin-top: 5px; box-shadow: 0 4px 6px rgba(0,0,0,0.1);">
                            <!-- Результаты поиска будут здесь -->
                        </div>
                    </div>

                    <!-- Выбранные пары -->
                    <div id="selectedPairs" style="margin-top: 15px;">
                        <h4>Выбранные пары:</h4>
                        <div id="selectedPairsList" style="display: flex; flex-wrap: wrap; gap: 10px; margin-top: 10px;">
                            <!-- Выбранные пары будут отображаться здесь -->
                        </div>
                    </div>
                </div>                <div style="margin-top: 20px;">
                    <button type="button" class="button" onclick="selectPopularPairs()">Добавить популярные пары</button>
                    <button type="button" class="button secondary" onclick="clearAllPairs()">Очистить все</button>
                </div>
            </div>

            <!-- Status Tab -->
            <div id="status" class="tab-content">
                <div class="instructions">
                    <h3>⚙️ Статус системы</h3>
                    <p>Проверьте статус всех компонентов системы перед запуском бота.</p>
                </div>

                <div id="systemStatus">
                    <!-- Статус будет загружен динамически -->
                </div>

                <div style="margin-top: 30px;">
                    <button type="button" class="button" onclick="checkSystemStatus()">
                        <span id="statusLoading" class="loading" style="display: none;"></span>
                        Проверить статус
                    </button>
                </div>
            </div>
        </form>

        <div style="padding: 30px; text-align: center; border-top: 1px solid #e0e0e0;">
            <button type="button" class="button success" onclick="saveConfiguration()" style="font-size: 1.1rem; padding: 15px 30px;">
                <span id="saveLoading" class="loading" style="display: none;"></span>
                💾 Сохранить настройки
            </button>
            <div id="saveResult" style="margin-top: 15px;"></div>
        </div>
    </div>

    <script>        // Глобальные переменные
        let availablePairs = [];
        let selectedPairs = [];
        let currentConfig = {};
        let filteredPairs = [];
        let selectedExchanges = ['binance', 'bybit']; // По умолчанию выбраны топ биржи

        // Переключение вкладок
        function showTab(tabName) {
            document.querySelectorAll('.tab-content').forEach(tab => {
                tab.classList.remove('active');
            });
            document.querySelectorAll('.nav-tab').forEach(btn => {
                btn.classList.remove('active');
            });
            
            document.getElementById(tabName).classList.add('active');
            event.target.classList.add('active');
        }

        // Загрузка конфигурации при старте
        document.addEventListener('DOMContentLoaded', async function() {
            await loadConfiguration();
            await loadAvailablePairs();
            renderSelectedPairs();
        });

        // Загрузка текущей конфигурации
        async function loadConfiguration() {            try {
                const response = await fetch('/api/admin/config');
                if (response.ok) {
                    currentConfig = await response.json();
                    fillForm(currentConfig);
                }
            } catch (error) {
                console.error('Ошибка загрузки конфигурации:', error);
            }
        }        // Заполнение формы данными
        function fillForm(config) {
            document.getElementById('telegramBotToken').value = config.telegramBotToken || '';
            document.getElementById('telegramChatId').value = config.telegramChatId || '';
            document.getElementById('analysisInterval').value = config.analysisInterval || '4h';
              // Загружаем выбранные биржи
            selectedExchanges = config.selectedExchanges || ['binance', 'bybit'];
            selectedExchanges.forEach(exchange => {
                const checkbox = document.getElementById(`exchange_${exchange}`);
                if (checkbox) {
                    checkbox.checked = true;
                }
            });
            updateSelectedExchanges();
            
            // Загружаем выбранные пары
            selectedPairs = config.analysisPairs || [];
        }// Загрузка доступных пар
        async function loadAvailablePairs() {
            if (selectedExchanges.length === 0) {
                availablePairs = [];
                filteredPairs = [];
                return;
            }

            try {
                const response = await fetch(`/api/admin/available-pairs?exchanges=${selectedExchanges.join(',')}`);
                if (response.ok) {
                    availablePairs = await response.json();
                    filteredPairs = [...availablePairs];
                }
            } catch (error) {
                console.error('Ошибка загрузки пар:', error);
                // Fallback список
                availablePairs = ['BTC/USDT', 'ETH/USDT', 'BNB/USDT', 'XRP/USDT', 'ADA/USDT', 'SOL/USDT'];
                filteredPairs = [...availablePairs];
            }
        }

        // Фильтрация пар по поисковому запросу
        function filterPairs() {
            const searchTerm = document.getElementById('pairSearch').value.toLowerCase();
            
            if (searchTerm.length === 0) {
                filteredPairs = [];
                renderDropdown();
                return;
            }

            filteredPairs = availablePairs.filter(pair => 
                pair.toLowerCase().includes(searchTerm) && !selectedPairs.includes(pair)
            );
            
            renderDropdown();
            showDropdown();
        }

        // Показать выпадающий список
        function showDropdown() {
            const dropdown = document.getElementById('pairDropdown');
            dropdown.style.display = 'block';
        }

        // Скрыть выпадающий список (с задержкой для кликов)
        function hideDropdown() {
            setTimeout(() => {
                const dropdown = document.getElementById('pairDropdown');
                dropdown.style.display = 'none';
            }, 200);
        }

        // Показать все доступные пары
        function showAllPairs() {
            filteredPairs = availablePairs.filter(pair => !selectedPairs.includes(pair));
            renderDropdown();
            showDropdown();
            document.getElementById('pairSearch').focus();
        }

        // Отрисовка выпадающего списка
        function renderDropdown() {
            const dropdown = document.getElementById('pairDropdown');
            
            if (filteredPairs.length === 0) {
                dropdown.innerHTML = '<div class="pair-dropdown-item" style="color: #666;">Пары не найдены</div>';
                return;
            }

            const html = filteredPairs.slice(0, 10).map(pair => 
                `<div class="pair-dropdown-item" onclick="addPair('${pair}')">${pair}</div>`
            ).join('');
            
            dropdown.innerHTML = html;
        }

        // Добавить пару в выбранные
        function addPair(pair) {
            if (!selectedPairs.includes(pair)) {
                selectedPairs.push(pair);
                selectedPairs.sort();
                renderSelectedPairs();
                
                // Очистить поиск
                document.getElementById('pairSearch').value = '';
                filteredPairs = [];
                renderDropdown();
            }
            hideDropdown();
        }

        // Удалить пару из выбранных
        function removePair(pair) {
            selectedPairs = selectedPairs.filter(p => p !== pair);
            renderSelectedPairs();
        }

        // Отрисовка выбранных пар
        function renderSelectedPairs() {
            const container = document.getElementById('selectedPairsList');
            
            if (selectedPairs.length === 0) {
                container.innerHTML = '<p style="color: #666; font-style: italic;">Пары не выбраны</p>';
                return;
            }

            const html = selectedPairs.map(pair => 
                `<div class="selected-pair-tag">
                    ${pair}
                    <span class="remove-pair" onclick="removePair('${pair}')">&times;</span>
                </div>`
            ).join('');
            
            container.innerHTML = html;
        }

        // Выбор популярных пар
        function selectPopularPairs() {
            const popularPairs = ['BTC/USDT', 'ETH/USDT', 'BNB/USDT', 'ADA/USDT', 'SOL/USDT', 'XRP/USDT', 'DOT/USDT', 'LINK/USDT'];
            
            popularPairs.forEach(pair => {
                if (availablePairs.includes(pair) && !selectedPairs.includes(pair)) {
                    selectedPairs.push(pair);
                }
            });
            
            selectedPairs.sort();
            renderSelectedPairs();
        }

        // Очистить все пары
        function clearAllPairs() {
            selectedPairs = [];
            renderSelectedPairs();
        }

        // Проверка токена
        async function validateToken() {
            const token = document.getElementById('telegramBotToken').value;
            if (!token) {
                showAlert('tokenValidationResult', 'Введите Bot Token', 'error');
                return;
            }

            showLoading('tokenValidationLoading', true);
              try {
                const response = await fetch('/api/admin/telegram/validate', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ botToken: token })
                });

                const result = await response.json();
                
                if (result.valid) {
                    showAlert('tokenValidationResult', 
                        `✅ Токен валиден! Бот: @${result.botInfo.username} (${result.botInfo.first_name})`, 
                        'success');
                } else {
                    showAlert('tokenValidationResult', `❌ ${result.error}`, 'error');
                }
            } catch (error) {
                showAlert('tokenValidationResult', `❌ Ошибка: ${error.message}`, 'error');
            } finally {
                showLoading('tokenValidationLoading', false);
            }
        }

        // Поиск Chat ID
        async function findChatId() {
            const token = document.getElementById('telegramBotToken').value;
            if (!token) {
                showAlert('chatIdResult', 'Сначала введите и проверьте Bot Token', 'error');
                return;
            }

            showLoading('chatIdLoading', true);
              try {
                const response = await fetch('/api/admin/telegram/find-chat', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ botToken: token })
                });

                const result = await response.json();
                
                if (result.found) {
                    document.getElementById('telegramChatId').value = result.chatId;
                    showAlert('chatIdResult', 
                        `✅ Chat ID найден: ${result.chatId}<br>Пользователь: ${result.firstName}${result.username ? ' (@' + result.username + ')' : ''}`, 
                        'success');
                } else {
                    showAlert('chatIdResult', `❌ ${result.error}`, 'error');
                }
            } catch (error) {
                showAlert('chatIdResult', `❌ Ошибка: ${error.message}`, 'error');
            } finally {
                showLoading('chatIdLoading', false);
            }
        }

        // Отправка тестового сообщения
        async function sendTestMessage() {
            const token = document.getElementById('telegramBotToken').value;
            const chatId = document.getElementById('telegramChatId').value;
            
            if (!token || !chatId) {
                showAlert('testMessageResult', 'Введите Bot Token и Chat ID', 'error');
                return;
            }

            showLoading('testMessageLoading', true);
              try {
                const response = await fetch('/api/admin/telegram/test', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ botToken: token, chatId })
                });

                const result = await response.json();
                
                if (result.success) {
                    showAlert('testMessageResult', '✅ Тестовое сообщение отправлено успешно!', 'success');
                } else {
                    showAlert('testMessageResult', `❌ ${result.error}`, 'error');
                }
            } catch (error) {
                showAlert('testMessageResult', `❌ Ошибка: ${error.message}`, 'error');
            } finally {
                showLoading('testMessageLoading', false);
            }
        }

        // Проверка статуса системы
        async function checkSystemStatus() {
            showLoading('statusLoading', true);
              try {
                const response = await fetch('/api/admin/status');
                const status = await response.json();
                
                let html = '<div class="alert info"><h3>Статус компонентов:</h3>';
                
                html += `<p><span class="status-indicator ${status.api ? 'green' : 'red'}"></span>API Сервер: ${status.api ? 'Работает' : 'Недоступен'}</p>`;
                html += `<p><span class="status-indicator ${status.cache ? 'green' : 'red'}"></span>Кеш: ${status.cache ? 'Активен' : 'Неактивен'}</p>`;
                html += `<p><span class="status-indicator ${status.exchange ? 'green' : 'red'}"></span>Биржа: ${status.exchange ? 'Доступна' : 'Недоступна'}</p>`;
                html += `<p><span class="status-indicator ${status.python ? 'green' : 'yellow'}"></span>Python сервис: ${status.python ? 'Работает' : 'Недоступен (опционально)'}</p>`;
                
                html += '</div>';
                
                document.getElementById('systemStatus').innerHTML = html;
            } catch (error) {
                showAlert('systemStatus', `❌ Ошибка проверки статуса: ${error.message}`, 'error');
            } finally {
                showLoading('statusLoading', false);
            }
        }        // Сохранение конфигурации
        async function saveConfiguration() {
            const config = {
                telegramBotToken: document.getElementById('telegramBotToken').value,
                telegramChatId: document.getElementById('telegramChatId').value,
                selectedExchanges: selectedExchanges,
                analysisInterval: document.getElementById('analysisInterval').value,
                analysisPairs: selectedPairs
            };

            // Валидация
            if (!config.telegramBotToken) {
                showAlert('saveResult', 'Bot Token обязателен', 'error');
                showTab('telegram');
                return;
            }

            if (!config.telegramChatId) {
                showAlert('saveResult', 'Chat ID обязателен', 'error');
                showTab('telegram');
                return;
            }

            if (selectedExchanges.length === 0) {
                showAlert('saveResult', 'Выберите хотя бы одну биржу', 'error');
                showTab('exchange');
                return;
            }

            if (selectedPairs.length === 0) {
                showAlert('saveResult', 'Выберите хотя бы одну валютную пару', 'error');
                showTab('analysis');
                return;
            }

            showLoading('saveLoading', true);
            
            try {
                const response = await fetch('/api/admin/config', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(config)
                });

                const result = await response.json();
                
                if (result.success) {
                    showAlert('saveResult', `✅ Настройки сохранены успешно!<br>Биржи: ${selectedExchanges.join(', ')}<br>Пары: ${selectedPairs.length}`, 'success');
                    currentConfig = config;
                } else {
                    showAlert('saveResult', `❌ Ошибка сохранения: ${result.error}`, 'error');
                }
            } catch (error) {
                showAlert('saveResult', `❌ Ошибка: ${error.message}`, 'error');
            } finally {
                showLoading('saveLoading', false);
            }
        }        // Полный список всех бирж из ccxt (106 бирж)
        const allExchanges = [
            'alpaca', 'apex', 'ascendex', 'bequant', 'bigone', 'binance', 'binancecoinm', 'binanceus', 'binanceusdm', 'bingx',
            'bit2c', 'bitbank', 'bitbns', 'bitfinex', 'bitflyer', 'bitget', 'bithumb', 'bitmart', 'bitmex', 'bitopro',
            'bitrue', 'bitso', 'bitstamp', 'bitteam', 'bittrade', 'bitvavo', 'blockchaincom', 'blofin', 'btcalpha', 'btcbox',
            'btcmarkets', 'btcturk', 'bybit', 'cex', 'coinbase', 'coinbaseadvanced', 'coinbaseexchange', 'coinbaseinternational', 'coincatch', 'coincheck',
            'coinex', 'coinlist', 'coinmate', 'coinmetro', 'coinone', 'coinsph', 'coinspot', 'cryptocom', 'cryptomus', 'defx',
            'delta', 'deribit', 'derive', 'digifinex', 'ellipx', 'exmo', 'fmfwio', 'gate', 'gateio', 'gemini',
            'hashkey', 'hitbtc', 'hollaex', 'htx', 'huobi', 'hyperliquid', 'independentreserve', 'indodax', 'kraken', 'krakenfutures',
            'kucoin', 'kucoinfutures', 'latoken', 'lbank', 'luno', 'mercado', 'mexc', 'modetrade', 'myokx', 'ndax',
            'novadax', 'oceanex', 'okcoin', 'okx', 'okxus', 'onetrading', 'oxfun', 'p2b', 'paradex', 'paymium',
            'phemex', 'poloniex', 'probit', 'timex', 'tokocrypto', 'tradeogre', 'upbit', 'vertex', 'wavesexchange', 'whitebit',
            'woo', 'woofipro', 'xt', 'yobit', 'zaif', 'zonda'
        ];

        // Категории бирж
        const exchangeCategories = {
            top: ['binance', 'bybit', 'okx', 'coinbase', 'kraken', 'kucoin', 'bitfinex', 'huobi', 'gate', 'mexc'],
            derivatives: ['bitmex', 'bybit', 'binanceusdm', 'binancecoinm', 'krakenfutures', 'kucoinfutures', 'phemex', 'deribit'],
            regional: ['binanceus', 'coinbaseadvanced', 'gemini', 'btcmarkets', 'bitflyer', 'upbit', 'bithumb', 'coinone'],
            altcoins: ['gate', 'mexc', 'bitget', 'bitmart', 'lbank', 'xt', 'probit', 'latoken'],
            all: allExchanges
        };

        // Описания бирж
        const exchangeDescriptions = {
            'alpaca': 'Alpaca - Американская биржа с алгоритмической торговлей',
            'apex': 'ApeX - Децентрализованная биржа деривативов',
            'ascendex': 'AscendEX - Глобальная цифровая биржа активов',
            'bequant': 'Bequant - Институциональная торговая платформа',
            'bigone': 'BigONE - Международная цифровая биржа активов',
            'binance': 'Binance - Крупнейшая криптовалютная биржа в мире',
            'binancecoinm': 'Binance COIN-M - Фьючерсы с монетной маржей',
            'binanceus': 'Binance US - Американское подразделение Binance',
            'binanceusdm': 'Binance USD-M - Фьючерсы с долларовой маржей',
            'bingx': 'BingX - Биржа с социальным трейдингом',
            'bit2c': 'Bit2C - Израильская криптовалютная биржа',
            'bitbank': 'bitbank - Японская криптовалютная биржа',
            'bitbns': 'BitBNS - Индийская криптовалютная биржа',
            'bitfinex': 'Bitfinex - Профессиональная торговая платформа',
            'bitflyer': 'bitFlyer - Крупнейшая биржа в Японии',
            'bitget': 'Bitget - Биржа с копи-трейдингом и фьючерсами',
            'bithumb': 'Bithumb - Южнокорейская криптовалютная биржа',
            'bitmart': 'BitMart - Глобальная цифровая биржа активов',
            'bitmex': 'BitMEX - Биржа деривативов и фьючерсов',
            'bitopro': 'BitoPro - Тайваньская криптовалютная биржа',
            'bitrue': 'Bitrue - Глобальная криптовалютная биржа',
            'bitso': 'Bitso - Мексиканская криптовалютная биржа',
            'bitstamp': 'Bitstamp - Европейская криптовалютная биржа',
            'bitteam': 'BitTeam - Биржа с командной торговлей',
            'bittrade': 'BitTrade - Японская криптовалютная биржа',
            'bitvavo': 'Bitvavo - Европейская криптовалютная биржа',
            'blockchaincom': 'Blockchain.com - Популярный кошелек и биржа',
            'blofin': 'Blofin - Профессиональная торговая платформа',
            'btcalpha': 'BTC-Alpha - Международная криптовалютная биржа',
            'btcbox': 'BTCBOX - Японская биржа биткоинов',
            'btcmarkets': 'BTC Markets - Австралийская криптовалютная биржа',
            'btcturk': 'BTCTurk - Турецкая криптовалютная биржа',
            'bybit': 'Bybit - Популярная биржа деривативов',
            'cex': 'CEX.IO - Мультифункциональная криптовалютная платформа',
            'coinbase': 'Coinbase - Американская публичная криптобиржа',
            'coinbaseadvanced': 'Coinbase Advanced - Продвинутая торговля',
            'coinbaseexchange': 'Coinbase Exchange - Профессиональная платформа',
            'coinbaseinternational': 'Coinbase International - Международное подразделение',
            'coincatch': 'CoinCatch - Деривативная торговая платформа',
            'coincheck': 'Coincheck - Японская криптовалютная биржа',
            'coinex': 'CoinEx - Глобальная цифровая биржа валют',
            'coinlist': 'CoinList - Платформа для токенизированных активов',
            'coinmate': 'CoinMate - Европейская криптовалютная биржа',
            'coinmetro': 'CoinMetro - Эстонская криптовалютная биржа',
            'coinone': 'CoinOne - Южнокорейская криптовалютная биржа',
            'coinsph': 'Coins.ph - Филиппинская криптовалютная биржа',
            'coinspot': 'CoinSpot - Австралийская криптовалютная биржа',
            'cryptocom': 'Crypto.com - Мобильная криптовалютная платформа',
            'cryptomus': 'Cryptomus - Криптовалютная платформа для платежей',
            'defx': 'DEFX - Биржа децентрализованных финансов',
            'delta': 'Delta - Биржа деривативов',
            'deribit': 'Deribit - Европейская биржа деривативов',
            'derive': 'Derive - DEX биржа',
            'digifinex': 'DigiFinex - Сингапурская цифровая биржа активов',
            'ellipx': 'EllipX - Биржа для алгоритмической торговли',
            'exmo': 'EXMO - Европейская криптовалютная биржа',
            'fmfwio': 'FMFW.io - Институциональная торговая платформа',
            'gate': 'Gate.io - Международная криптовалютная биржа',
            'gateio': 'Gate.io - Альтернативное название Gate.io',
            'gemini': 'Gemini - Американская регулируемая биржа',
            'hashkey': 'HashKey - Азиатская институциональная биржа',
            'hitbtc': 'HitBTC - Европейская криптовалютная биржа',
            'hollaex': 'HollaEx - Белая метка биржевого программного обеспечения',
            'htx': 'HTX - Глобальная криптовалютная биржа',
            'huobi': 'Huobi - Международная цифровая биржа активов',
            'hyperliquid': 'Hyperliquid - L1 блокчейн для деривативов',
            'independentreserve': 'Independent Reserve - Австралийская/Новозеландская биржа',
            'indodax': 'Indodax - Индонезийская криптовалютная биржа',
            'kraken': 'Kraken - Американская криптовалютная биржа',
            'krakenfutures': 'Kraken Futures - Деривативная платформа Kraken',
            'kucoin': 'KuCoin - "Народная биржа"',
            'kucoinfutures': 'KuCoin Futures - Фьючерсная платформа KuCoin',
            'latoken': 'LATOKEN - Мультиактивная торговая платформа',
            'lbank': 'LBank - Международная криптовалютная биржа',
            'luno': 'Luno - Биржа в Африке и Юго-Восточной Азии',
            'mercado': 'Mercado Bitcoin - Бразильская криптовалютная биржа',
            'mexc': 'MEXC - Глобальная криптовалютная биржа',
            'modetrade': 'Mode Trade - Современная торговая платформа',
            'myokx': 'MyOKX - Персонализированная платформа OKX',
            'ndax': 'NDAX - Канадская криптовалютная биржа',
            'novadax': 'NovaDAX - Бразильская криптовалютная биржа',
            'oceanex': 'OceanEx - Криптовалютная биржа VeChain',
            'okcoin': 'OKCoin - Глобальная криптовалютная биржа',
            'okx': 'OKX - Ведущая криптовалютная биржа и Web3 компания',
            'okxus': 'OKX US - Американское подразделение OKX',
            'onetrading': 'One Trading - Европейская криптовалютная биржа',
            'oxfun': 'OXFun - Биржа деривативов',
            'p2b': 'P2B - Международная криптовалютная биржа',
            'paradex': 'Paradex - Децентрализованная биржа',
            'paymium': 'Paymium - Французская биржа биткоинов',
            'phemex': 'Phemex - Биржа криптовалютных деривативов',
            'poloniex': 'Poloniex - Американская криптовалютная биржа',
            'probit': 'ProBit - Глобальная криптовалютная биржа',
            'timex': 'TimeX - Гибридная децентрализованная биржа',
            'tokocrypto': 'Tokocrypto - Индонезийская криптовалютная биржа',
            'tradeogre': 'TradeOgre - Биржа для альткоинов',
            'upbit': 'Upbit - Южнокорейская криптовалютная биржа',
            'vertex': 'Vertex - Децентрализованная биржа деривативов',
            'wavesexchange': 'Waves.Exchange - Децентрализованная биржа',
            'whitebit': 'WhiteBIT - Европейская криптовалютная биржа',
            'woo': 'WOO - Биржа с глубокой ликвидностью',
            'woofipro': 'WooFi Pro - Профессиональная торговая платформа',
            'xt': 'XT.COM - Глобальная криптовалютная торговая платформа',
            'yobit': 'YoBit - Российская криптовалютная биржа',
            'zaif': 'Zaif - Японская криптовалютная биржа',
            'zonda': 'Zonda - Европейская криптовалютная биржа'
        };

        let currentVisibleExchanges = exchangeCategories.top; // По умолчанию показываем ТОП-10

        // Инициализация при загрузке страницы
        document.addEventListener('DOMContentLoaded', function() {
            showExchangeCategory('top');
            updateSelectedExchanges();
        });

        // Показать биржи определенной категории
        function showExchangeCategory(category) {
            currentVisibleExchanges = exchangeCategories[category] || allExchanges;
            renderExchanges(currentVisibleExchanges);
        }

        // Фильтрация бирж по поиску
        function filterExchanges() {
            const searchTerm = document.getElementById('exchangeSearch').value.toLowerCase();
            const filtered = currentVisibleExchanges.filter(exchange => 
                exchange.toLowerCase().includes(searchTerm) || 
                exchangeDescriptions[exchange].toLowerCase().includes(searchTerm)
            );
            renderExchanges(filtered);
        }

        // Отрисовка бирж в контейнере
        function renderExchanges(exchanges) {
            const container = document.getElementById('exchangesContainer');
            container.innerHTML = '';

            exchanges.forEach(exchange => {
                const exchangeDiv = document.createElement('div');
                exchangeDiv.className = 'exchange-checkbox';
                exchangeDiv.innerHTML = `
                    <input type="checkbox" id="exchange_${exchange}" value="${exchange}" onchange="updateSelectedExchanges()">
                    <label for="exchange_${exchange}">
                        <strong>${exchange.charAt(0).toUpperCase() + exchange.slice(1)}</strong>
                        <br><small>${exchangeDescriptions[exchange] || exchange}</small>
                    </label>
                `;
                container.appendChild(exchangeDiv);
            });
        }

        // Обновление выбранных бирж
        function updateSelectedExchanges() {
            selectedExchanges = [];
            
            allExchanges.forEach(exchange => {
                const checkbox = document.getElementById(`exchange_${exchange}`);
                if (checkbox && checkbox.checked) {
                    selectedExchanges.push(exchange);
                    checkbox.parentElement.classList.add('selected');
                } else if (checkbox) {
                    checkbox.parentElement.classList.remove('selected');
                }
            });

            // Обновляем счетчик
            const countElement = document.getElementById('selectedCount');
            if (countElement) {
                countElement.textContent = `Выбрано: ${selectedExchanges.length}`;
            }

            // Обновляем доступные пары при изменении бирж
            if (selectedExchanges.length > 0) {
                loadAvailablePairs();
            } else {
                availablePairs = [];
                filteredPairs = [];
                renderDropdown();
            }
        }
        
        // Выбрать ТОП-5 бирж
        function selectTopExchanges() {
            const topExchanges = ['binance', 'bybit', 'okx', 'coinbase', 'kraken'];
            
            // Очищаем текущий выбор
            clearAllExchanges();
            
            // Выбираем топ биржи
            topExchanges.forEach(exchange => {
                const checkbox = document.getElementById(`exchange_${exchange}`);
                if (checkbox) {
                    checkbox.checked = true;
                }
            });
            
            updateSelectedExchanges();
        }

        // Выбрать все видимые биржи
        function selectAllVisible() {
            const container = document.getElementById('exchangesContainer');
            const checkboxes = container.querySelectorAll('input[type="checkbox"]');
            
            checkboxes.forEach(checkbox => {
                checkbox.checked = true;
            });
            
            updateSelectedExchanges();
        }
        
        // Очистить выбор бирж
        function clearAllExchanges() {
            allExchanges.forEach(exchange => {
                const checkbox = document.getElementById(`exchange_${exchange}`);
                if (checkbox) {
                    checkbox.checked = false;
                }
            });
            
            updateSelectedExchanges();
        }

        // Вспомогательные функции
        function showAlert(elementId, message, type) {
            const element = document.getElementById(elementId);
            element.innerHTML = `<div class="alert ${type}">${message}</div>`;
            
            // Автоматически скрыть сообщение через 10 секунд
            if (type === 'success') {
                setTimeout(() => {
                    element.innerHTML = '';
                }, 10000);
            }
        }

        function showLoading(elementId, show) {
            const element = document.getElementById(elementId);
            element.style.display = show ? 'inline-block' : 'none';
        }
    </script>
</body>
</html>
